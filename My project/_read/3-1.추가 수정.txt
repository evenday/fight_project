회전시 기울기 반환 함수 변경
float GetLeanValue()
{
        if (cam_data.mouse_x > 0.01f)
        {
            return -max_lean;
        }
        else if (cam_data.mouse_x < -0.01f)
        {
            return max_lean;
        }
        else
            return  0.0f;
}

-> float GetMouseRotLeanValue(float Mouse_rot_value);
    {
        if (Mouse_rot_value > 0.01f)
        {
            return -max_lean;
        }
        else if (Mouse_rot_value < -0.01f)
        {
            return max_lean;
        }
        else
            return  0.0f;
    }
==============================================================
player 애니메이션 bool값이 아닌 속도(velocity)값으로 설정





