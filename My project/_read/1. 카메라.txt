현재 목표
1. 메인 카메라는 캐릭터의 회전의 영향을 받지 않는 회전을 하며
2. 줌 기능을 가지고 있고
3. 바닥과 벽에 충돌시 충돌물체 밖으로 나가지 않고 줌을 시킴 (맵 밖으로 나가지 못하게 함) 
(physics를 이용 || collider(Trigger)사용)
참고
zzz
캐릭터 이동 방식
카메라와 캐릭터가 따로 회전값을 가짐



1. 카메라 위치 -> 캐릭터 위치
   카메라 회전 -> 마우스x,y
{
1-1. 카메라를 캐릭터 자식으로 놓기 					X
	-> 캐릭터가 회전시 카메라도 같이 돌아감

1-2. 카메라를 따로 관리(캐릭터 쪽 방향, 거리)
-> 캐릭터에 Carmera Manager오브젝트 추가(
이 오브젝트기준으로 카메라 위치나 회전 설정 mainCamera에 관리 		X
	-> 캐릭터 회전 관련 할때 캐릭터의 영향을 받음(해결X)

1-2-1 캐릭터 자식이 아닌 단독 객체의 CameraManager오브젝트 생성		X
오브젝트 위치 -> 캐릭터 중심위치
	->x축 회전과 y축 회전이 동시에 계산되서 회전이 이상하게됨(좌우회전 축(Y)이 기울어진채 회전)
	(x축으로 회전한후 y축회전 축이 서로 고정된 상태가 아닌 움직인 상태에서 적용됨)

1-2-2 CameraManager에 x축 y축 을 담당 하는 Object 자식으로
CameraManger(XAxis(YAxis(MainCamera)))

1-2-2-1 마우스휠로 카메라 거리 이동
기존-> CameraManager(Object), MainCamera의 위치를 Vector3로 관리
교체--> float거리 값으로 받고, CameraManager(Object)를 회전 시킴*******
(Quaternion.LookRotation()으로 CameraManager )
(그전-> 카메라 위치를 Vector3로 지정)
(교체-> 카메라 위치를 float값으로 받아 일정 거리 만큼 떨어지게 설정)
}

1-2-2-2 마우스휠로 zoom(in out)
확대나 축소 할때 뚝뚝 끈기는 느낌이 있어 부드럽게 하기
smoothdamp()사용(lerp-> 비율값을 계속 계산 해줘야해서 X(deltaTime / durtion))
lerp
a와 b의 사이의 t만큼의 비율의 값을 반환 하는 함수 

1-2-2-2-1 카메라 벽에 충돌시 카메라 거리 줄어듬(distance거리 조절)
checksphere()->바닥이나 벽에 계속 충돌 하고 있을 시 떨림
-> checksphere는 닷는 즉시 반응
 카메라 주변 벽에 다았는지 확인 하는 형식필요 
(checksphere와 집접적으로 충돌 하지 않는 형식)
-> 안정거리? (충돌 되자 마자 일정거리 줄어들고 다시 원레 거리로 돌아가야 함
	-> 여기서 충돌되서 줄어들었다가 원래 거리로 돌아갈려고 하기 때문에 흔들림)
CameraZoom에서 zoom_size로 카메라 거리 관리(distance가 줄어들어도 zoom_size에 기존 zoom거리가
저장되어 있어 다시 기존 거리로 돌아갈려고함)


collider형식이 아닌 trigger느낌 필요

1-2-2-2-2 		X
physics.SphereCast() 		
-> physics.checksphere와 다를것 없음 내각 했던 Trigger의 느낌의 함수가 아니였음
그리고 한번만 작동 안함 충돌 
*** 한번 충돌 처리하고 나서는 바닥을 통과 해버림
->  spherecast는 충돌처리되면 true처리를 한번만 진행 
(while문사용해야함->에디터 팅김 절대 하지 말것)

1-2-2-2-3
collider_zoom_size(float) 추가로 벽충돌과 마우스 휠로 줌크기를 따로 관리
spherecast()로 줌 이동 관리
충돌 적용시 -> collider_zoom_size 
마우스 휠-> zoom_size
마우스 충돌 된후 충돌 하지 않을 collider_zoom_size = zoom_size로 초기화 (X : in/out 왔다 갔다함
-> 확대되었다가 spherecast끝나면 원래 줌으로 돌아갈려고 하는데 또 충돌 처리되어서 다시 줌될려는거 반복)

1-2-2-2-4
checksphere 와 spherecast동시 사용				X
1. spherecast로 거리 안에 충돌 되었을시 true로 변환 
2. true인 상황에서 checksphere가 충돌 처리 되면 이떄 줌되게

1-2-2-2-5
Spherecast의 시작 위치가 변화는거 막기		O
변화(main_tranform.position -> (CameraManager)transform.position)
RaycastHit.distance를 CameraSmoothMove()의 이동 목표자리로함
충돌 적용시 -> collider_zoom_size 
마우스 휠-> zoom_size

1-2-2-2-5-1
회전을 빨리하면 벽을 통과함 
spherecast로 벽이나 바닥에 충돌전에 적용
(확인 거리는 CameraManager 부터 카메라 뒤(3.0) 만큼 뒤를 확인)
checksphere로 벽이나 바닥에 통과시 적용
(카메라 주심으로 부터 0.5f 거리(반지름)으로 체크)
->만약 checksphere 충돌 되었을 시 

CameraZoom()
변경 
충돌 시 zoom 기능 추가
마우스 휠로 in/out과 충돌 처리시 zoom target을 따로 관리

Checksphere(벽외부로 나가는거 방지)
->충돌 될시 distance -= 1 (smoothdamp X -> smoothdamp로 이동시 천천히 이동되어서 외부가 보임)

spherecast
start: CameraManager위치
direction: CameraManager -> MainCamera
MaxDistance: MainCamera + 3.0f 까지 
만약 충돌시 
충돌위치에서 3.0f를 뺀거리를 줌 target으로 
else
input->mouse_wheel값으로 zoom거리 조정 

MouseZoom(), ColZoom() 추가 
각각 입력조작과 충돌시 줌 기능을 가짐 
CameraZoom()에서 사용


